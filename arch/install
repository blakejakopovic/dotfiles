#!/usr/bin/env bash
#
# ~/.dotfiles/arch/install

[ -z "$DOTFILESPATH" ] && { echo "Error: ${0} cannot be called directly"; exit 1; }

DOTFILESPATH="${DOTFILESPATH}/arch"

echo "==> Installing Arch dotfiles..."

echo "${PADDING}[none]"
