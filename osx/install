#!/bin/bash

BASEDIR="$HOME/.dotfiles/osx"
LINKCMD="ln -is"

echo "Installing osx dotfiles..."

# Symlink dotfiles
${LINKCMD} ${BASEDIR}/.gitconfig ~/.gitconfig
${LINKCMD} ${BASEDIR}/.gitignore_global ~/.gitignore_global