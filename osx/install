#!/usr/bin/env bash
#
# ~/.dotfiles/osx/install

[ -z "$DOTFILES_DIR" ] && { echo "Error: ${0} cannot be called directly"; exit 1; }

DOTFILES_DIR="${DOTFILES_DIR}/osx"

echo "==> Installing macOS dotfiles..."

r_symlink .git-credentials