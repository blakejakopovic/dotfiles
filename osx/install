#!/usr/bin/env bash
#
# ~/.dotfiles/osx/install

[ -z "$DOTFILESPATH" ] && { echo "Error: ${0} cannot be called directly"; exit 1; }

DOTFILESPATH="${DOTFILESPATH}/osx"

echo "==> Installing macOS dotfiles..."

r_symlink .git-credentials
